import { Router } from "express";

const router = Router();

// TODO: Define transaction routes (e.g., /buy, /sell, /deposit, /withdraw)
// router.route("/buy").post(buyStock)

export default router;
